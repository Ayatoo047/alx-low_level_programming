#include <stdio.h>
/**
 * main - Entry piont
 *
 * Return: Always 0 (Success)
 */
int main(void)

{
	int digit;

	for (digit = 0; digit <= 9; digit++)
	{
		printf(digit);
	}
	putchar('\n');

	return (0);
}
