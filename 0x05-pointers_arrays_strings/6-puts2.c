#include "main.h"

/**
 * print_rev - to print in reverse order
 * @s: pointer to  int
 *
 * Return: Always return 0.
 */
void print_rev(char *s)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if (a % 2 == 0)
		_putchar (str[a]);
	}
	_putchar ('\n');
}
