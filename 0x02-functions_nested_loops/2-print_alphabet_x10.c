#include <stdio.h>

/**
 * print_alphabet - print alphabet
 *
 * Return: Always 0 for success
 */
char print_alphabet(void)
{
	while (i < 10, i++)
	{
		for (i = 'a'; i <= 'z'; i++)
			putchar(i)
			putchar("\n")

	}
}
